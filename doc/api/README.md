# Sphinx HTML document generation
We used sphinx to generate document in HTML format, the theme is `read the doc` theme
```bash
SQLUSER="root" SQLPASS="" SQLHOST="localhost" SQLPORT="9030" ui="streamlit" make html
```
